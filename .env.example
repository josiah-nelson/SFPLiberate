# SFPLiberate Environment Configuration
# Copy this file to .env and customize for your deployment

# =============================================================================
# DEPLOYMENT MODE
# =============================================================================
# Options: "standalone" (Docker with SQLite) or "appwrite" (Cloud deployment)
# DEPLOYMENT_MODE=standalone

# =============================================================================
# ESPHOME BLUETOOTH PROXY
# =============================================================================
# Enable ESPHome proxy for iOS/Safari users without Web Bluetooth support
# Set to "true" to enable, "false" to disable (default)
# When enabled, use docker-compose.esphome.yml for host networking
ESPHOME_PROXY_MODE=false

# Optional: Manual ESPHome proxy configuration
# Uncomment if mDNS discovery doesn't work in your environment
# ESPHOME_PROXY_HOST=192.168.1.100
# ESPHOME_PROXY_PORT=6053
# ESPHOME_PROXY_NAME=my-esphome-proxy

# =============================================================================
# SFP WIZARD DEVICE PROFILE (BLE UUIDs)
# =============================================================================
# Default UUIDs for firmware v1.0.10
# These are auto-discovered via BLE and saved to localStorage in the frontend
# Set these to pre-seed the profile on startup (optional)
SFP_SERVICE_UUID=8E60F02E-F699-4865-B83F-F40501752184
SFP_WRITE_CHAR_UUID=9280F26C-A56F-43EA-B769-D5D732E1AC67
SFP_NOTIFY_CHAR_UUID=DC272A22-43F2-416B-8FA5-63A071542FAC

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# Backend host port (exposed to host machine)
# BACKEND_HOST_PORT=8081

# Frontend host port (main application access point)
# FRONTEND_HOST_PORT=8080

# Docker build arguments
# PYTHON_VERSION=3.14
# POETRY_VERSION=1.8.5
# VERSION=dev

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
# Database file path (inside container)
# DATABASE_FILE=/app/data/sfp_library.db

# Module submissions directory
# SUBMISSIONS_DIR=/app/data/submissions

# Logging level: debug, info, warning, error, critical
# LOG_LEVEL=info

# Environment: development, staging, production
# ENVIRONMENT=production

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Backend URL (used by Next.js rewrites for API proxying)
# Default uses Docker internal DNS
# BACKEND_URL=http://backend:80

# Frontend port (inside container)
# FRONTEND_PORT=3000

# =============================================================================
# APPWRITE CLOUD DEPLOYMENT (Optional)
# =============================================================================
# Only needed if DEPLOYMENT_MODE=appwrite
# APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
# APPWRITE_PROJECT_ID=your-project-id
# APPWRITE_DATABASE_ID=your-database-id
# APPWRITE_COLLECTION_ID=your-collection-id
# APPWRITE_API_KEY=your-api-key
# NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
# NEXT_PUBLIC_APPWRITE_PROJECT_ID=your-project-id

# =============================================================================
# ADVANCED CONFIGURATION
# =============================================================================
# Enable bundle analyzer for frontend builds
# BUNDLE_ANALYZER_ENABLED=false

# Docker network subnet (bridge mode)
# Ensure this doesn't conflict with your LAN
# DOCKER_SUBNET=172.25.0.0/24

# Domain for Traefik reverse proxy (if using)
# DOMAIN=localhost
